{{#if stages}}
    <div class="{{b 'step' expanded=expanded}}">
        <div class="{{b 'step' 'header' hasContent=true stage=true}}">
            <span class="step__arrow block__arrow">{{angle}}</span>
            <span class="step__title">
              {{name}}
            </span>
        </div>
        <div class="{{b 'step' 'content'}}">
            {{#each stages}}
                {{#if name}}
                    <div class="{{b 'step'}}">
                        <div class="{{b 'step' 'header' hasContent=hasContent stage=true}}">
                            {{#if hasContent}}
                                <span class="{{b 'step' 'arrow'}} text_status_{{status}} block__arrow">{{arrow status fat=true}}</span>
                            {{else}}
                                <span class="{{b 'step' 'status'}} text_status_{{status}}">{{allure-icon status}}</span>
                            {{/if}}
                            <div class="{{b 'step' 'title'}}">
                              {{name}}
                              {{> ../../blocks/step-stats/step-stats . baseUrl=../../baseUrl}}
                            </div>
                        </div>
                        <div class="{{b 'step' 'content'}}">
                            {{> ../../blocks/parameters-table/parameters-table parameters=parameters }}
                            {{~> steps-list steps=steps baseUrl=../baseUrl}}
                            {{#each attachments}}
                                {{> ../../blocks/attachment-row/attachment-row . baseUrl=../../baseUrl}}
                            {{/each}}
                            {{#if shouldDisplayMessage}}
                                {{~> ../../blocks/status-details/status-details .}}
                            {{/if}}
                        </div>
                    </div>
                {{else}}
                    {{~> steps-list steps=steps baseUrl=../baseUrl}}
                    {{#each attachments}}
                        {{> ../../blocks/attachment-row/attachment-row . baseUrl=../../baseUrl}}
                    {{/each}}
                    {{#if shouldDisplayMessage}}
                        {{~> ../../blocks/status-details/status-details .}}
                    {{/if}}
                {{/if}}
            {{/each}}
        </div>
    </div>
{{/if}}
