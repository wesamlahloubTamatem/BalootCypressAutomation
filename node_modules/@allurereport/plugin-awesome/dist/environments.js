export const filterEnv = (env, filter) => {
    return (testResult) => {
        if (testResult.environment !== env) {
            return false;
        }
        return filter ? filter(testResult) : true;
    };
};
